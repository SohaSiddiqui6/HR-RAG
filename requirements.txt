# pyproject.toml is the source of truth. Kept for platforms that install from
# requirements.txt; regenerate with:  uv export --no-dev -o requirements.txt

# --- Web ---
fastapi>=0.115,<1
uvicorn[standard]>=0.30,<1
jinja2>=3.1,<4
python-multipart>=0.0.9,<1
python-dotenv>=1.0,<2

# --- Ingestion (Docling PDF parsing + chunking) ---
docling>=2.0,<3
transformers>=4.40,<5
pymupdf>=1.24,<2

# --- Vector store (Chroma Cloud, dense + sparse/BM25) ---
chromadb>=1.4,<2                 # server-side hybrid Search API
snowballstemmer>=2.2,<3          # tokenizer for ChromaBm25EmbeddingFunction

# --- Retrieval + generation ---
langchain>=0.3,<0.4
langchain-core>=0.3,<0.4
langchain-community>=0.3,<0.4
langchain-openai>=0.3,<0.4
langchain-cohere>=0.3,<0.4
cohere>=5.5,<6
openai>=1.30,<2

# --- Testing ---
pytest>=8.0,<9
httpx>=0.27,<1                   # FastAPI TestClient
