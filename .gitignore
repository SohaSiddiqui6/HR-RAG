# Python / OS
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.vscode/
.idea/
.DS_Store

# Secrets
.env
app.env          # rendered for `docker run --env-file` on deploy (see docs/DEPLOY.md)
*.pem            # EC2 SSH key pairs

# Backend — local state (regenerated) + caches
backend/evaluation/results.json
models_cache/
.docling/

# Frontend
frontend/node_modules/
frontend/dist/
frontend/.env
hr-rag-key.pem
app.env
